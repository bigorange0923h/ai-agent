# App

最终 Agent MVP 主项目目录。

规划：

- `backend/`：后端服务，后续承接文档导入、RAG 问答、交接包生成、日志查询等接口
- `frontend/`：简单演示页面，后续用于展示文档导入、问答结果、来源引用和交接包生成
- `docker/`：Docker Compose、环境变量样例和部署配置

当前目录只放最终可演示项目，不放每日临时代码。每日学习实验继续放在 `days/dayXX/code/`。
